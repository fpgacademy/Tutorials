If you cannot change the PATH environment variable so that it includes the
folder where the required GNU make.exe is installed, then you might want to
use the provided gmake.bat file as a way of running make. In this case to make
a target such as COMPILE, you would execute ./gmake COMPILE.
